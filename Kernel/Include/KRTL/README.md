# KRTL
Kernel Runtime Library (KRTL) is a set of utilities that the kernel makes use of often and in many places.  
It is not a separate library per se (built with kernel) but named as such because it serves as such.
